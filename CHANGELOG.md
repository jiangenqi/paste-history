# Changelog

本文档记录 PasteHistory 的版本变更。此前为内部开发迭代，未公开发布。

## [3.0.3] - 2026-08-14

### 新增

- **深色模式**：设置面板"通用"组新增浅色/深色切换，即时生效并持久化；主窗口、设置面板、卡片、弹窗、详情窗口全部跟随；雾紫灰低饱和配色
- **深色眼睛**：深色模式下眼睛切换为炭黑眼球 + 金虹膜（夜间猫眼风格），小/中/大尺寸均生效
- 任务栏身份：显式设置 AppUserModelID，任务栏右键菜单不再显示 "Electron"

### 变更

- 深色模式下分类标签/操作按钮为幽灵风格；右下角按钮选中态为实心填充；置顶卡片带主色边条
- zZ（睡眠指示）窗口与字号随眼睛大小等比缩放
- 眼睛惊醒时 zZ 立即消失（出现保留延迟淡入，消失不再延迟）

### 修复

- 切换眼睛大小不再清除深色模式 class（此前小/大尺寸会变回浅色眼睛）
- 深色模式开发过程中浅色模式保持完全不变（深色效果全部隔离在 dark-mode 覆盖层）

## [3.0.2] - 2026-08-14

### 变更

- 眼睛模式呼出后的点击保护窗口从 2 秒缩短为 0.3 秒：单击眼睛可**立即**关闭并返回主界面，同时仍过滤眼睛刚出现瞬间的误点
- 点击冷却仍由 IPC 确定性重置，仅响应鼠标左键

## [3.0.1] - 2026-08-14

### 修复

- 修复眼睛模式偶发"呼出后立即消失"：呼出时的点击冷却重置改为 IPC 确定性下发（`eye:resetCooldown`）。原方案依赖 `visibilitychange` 事件，Electron 后台窗口场景下该事件不可靠，导致冷却计时停留在页面加载时刻、早已过期，首次点击即触发"单击关闭"
- 眼睛的"单击关闭"与拖拽现在仅响应鼠标左键，右键不再误触发关闭

## [3.0.0] - 2026-08-12

首个公开发布版本。

### 新增

- **多分类系统**：新增 `categories` 表与 `clip_categories` 关联表，一条收藏可同时属于多个分类；分类标签栏支持新建、重命名、删除、过滤，旧数据自动迁移
- **文件复制记录**：支持记录资源管理器中复制的文件（CF_HDROP），显示系统图标与文件名，点击可复制回剪贴板，右键可打开文件或在文件夹中显示
- **右键详情窗口**：文字 / 图片 / 文件记录均可右键在新窗口查看完整内容
- **眼睛模式**：桌面出现一只跟随鼠标的眼睛，鼠标静止超过设定时长后逐渐"犯困入睡"；支持拖拽移动位置
- **收藏夹视图**：底部工具栏切换，只看已收藏记录
- **导出历史**：全部记录导出为排版整齐的文本文件，含类型统计、置顶/收藏统计与分类归属，CJK 宽字符对齐
- **开机自启动**开关、**最大记录数**设置（200/500/1000 或自定义）

### 变更

- 设置面板重构为 Apple 风格分组卡片（通用 / 快捷操作 / 眼睛模式 / 数据 / 危险操作）
- 卸载功能重写为无窗口 PowerShell 方案，清理桌面与开始菜单快捷方式
- 点击卡片复制后窗口保持打开（不再自动隐藏），以 toast 提示结果
- 文件剪贴板读写改用 .NET Clipboard API（`-STA` 模式 + UTF-8 输出），解决 Electron 原生 API 对 CF_HDROP 支持不全的问题

### 修复

- 修复 sql.js `db.export()` 导致 `last_insert_rowid()` 归零、新记录 id 为 0 的根因（新建分类不归入、会话内无法置顶/收藏）
- 修复文件复制后中文文件名 GBK 乱码
- 修复文件卡片图标重启后丢失（新增 `app:getFileIcons` 异步补齐）
- 修复新建分类弹窗数据过期、多分类 tab 同时选中、取消收藏残留分类等分类系统缺陷
- 修复设置页面滚动卡顿（`-webkit-app-region: no-drag` 根因）
- 修复数据库脏数据迁移：清理关联到不存在分类的历史残留
